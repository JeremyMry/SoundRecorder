{
  "name": "sound-recorder-api",
  "version": "1.0.0",
  "description": "webservice to transcribe/summarize audio input",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "file:AUTHORS.txt",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
